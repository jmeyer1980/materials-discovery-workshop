services:
  materials-discovery:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PYTHONPATH=/app
      - DOCKER_DEBUG=1
    command: python gradio_app.py
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G
