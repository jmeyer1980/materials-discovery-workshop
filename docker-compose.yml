version: '3.8'

services:
  materials-discovery:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - PYTHONPATH=/app
    command: python gradio_app.py
