version: '3.8'

services:
  materials-discovery:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - PYTHONPATH=/app
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true
